# GenioAI 分离部署域名与备案分析

## 一、当前域名配置

| 用途 | 域名 | 位置 | 说明 |
|------|------|------|------|
| API 后端 | `genioai.appbobo.com` | Nginx 反代 → genioAI_server:8200 | 主 API 入口 |
| CDN 图片 | `cdn.appbobo.com` | Cloudflare R2 CDN | 生成图片的访问地址 |
| SSL 证书 | Let's Encrypt | `/etc/letsencrypt/live/genioai.appbobo.com/` | 自动签发 |
| ComfyUI 服务 | `ai-comfyui.domob-inc.com` | 外部服务 | 图片生成后端 |
| OSS 存储 | `visionai-ugc.domob.cn` | 阿里云 OSS 北京 | 文件存储 |
| 全球 CDN | `visionai-ugc-global.domobcdn.com` | 腾讯云 COS 新加坡 | 全球分发 |

主域名为 `appbobo.com`。

---

## 二、ICP 备案要求

### 2.1 核心规则

**中国大陆的规则：域名解析到国内服务器 IP，就必须完成 ICP 备案。**

这不是可选的。阿里云、腾讯云等国内云服务商会主动拦截未备案域名的 HTTP/HTTPS 访问，未备案域名将无法通过域名正常访问国内服务器。

### 2.2 需要备案的部分

分离部署后，`genioai.appbobo.com` 需要解析到国内服务器，因此：

| 需要完成的事项 | 说明 |
|--------------|------|
| `appbobo.com` 主域名 ICP 备案 | 必须在购买服务器的云服务商处提交备案 |
| 备案主体 | 企业或个人（个人备案对经营性网站有限制） |
| 备案地点 | 购买哪家云服务器就在哪家备案（阿里云服务器 → 阿里云备案） |
| 备案周期 | 通常 1-2 周 |

### 2.3 不需要备案的部分

| 域名/服务 | 是否需要备案 | 原因 |
|-----------|------------|------|
| `cdn.appbobo.com` | 不需要 | 指向 Cloudflare CDN，不在国内服务器 |
| ai-brain gRPC 接口 | 不需要 | 在日本服务器，通过 WireGuard 内网 IP 通信，不绑定域名 |
| `ai-comfyui.domob-inc.com` | 不需要 | 外部第三方服务 |
| `visionai-ugc.domob.cn` | 不需要 | 阿里云 OSS 域名，属于云服务商托管 |
| 各 AI API 域名 | 不需要 | 第三方服务（DeepSeek、Gemini、Kling 等） |

### 2.4 如果 `appbobo.com` 已经备案

如果主域名已完成 ICP 备案，只需要在备案系统中添加新的子域名解析记录即可，通常不需要重新走完整备案流程。具体操作视云服务商而定，一般在备案管理后台添加网站或修改接入信息。

---

## 三、域名变更分析

### 3.1 域名本身不需要修改

分离部署**不需要更换域名**，只需修改 DNS 解析指向：

```
修改前：genioai.appbobo.com  →  A 记录  →  日本服务器 IP
修改后：genioai.appbobo.com  →  A 记录  →  国内服务器 IP
```

### 3.2 不需要改动的配置

以下所有位置引用的域名保持不变，无需修改：

| 位置 | 文件 | 说明 |
|------|------|------|
| 服务端配置 | `conf/server.prod.yaml` | CDN 地址 `cdn.appbobo.com` 等不变 |
| 部署脚本 | `deploy.sh` / `deploy-zero-downtime.sh` | API 地址引用不变 |
| 前端配置 | `NUXT_PUBLIC_API_BASE_URL` | 仍然是 `https://genioai.appbobo.com` |
| 客户端/App | 内置的 API 地址 | 不需要发版更新 |
| CDN 配置 | Cloudflare R2 | `cdn.appbobo.com` 指向不变 |

**对用户完全无感知。**

---

## 四、SSL 证书处理

### 4.1 当前证书配置

**Nginx 配置**（来源：`nginx-backend-updated.conf`）：

```nginx
server {
    listen 443 ssl http2;
    server_name genioai.appbobo.com;

    ssl_certificate /etc/letsencrypt/live/genioai.appbobo.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/genioai.appbobo.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}
```

### 4.2 迁移步骤

DNS 解析切换到国内服务器后，在国内服务器上重新申请证书：

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx

# 申请证书（DNS 必须已解析到当前服务器）
sudo certbot certonly --nginx -d genioai.appbobo.com

# 设置自动续期
sudo certbot renew --dry-run
```

证书路径与原来一致（`/etc/letsencrypt/live/genioai.appbobo.com/`），Nginx 配置文件可以直接从日本服务器复制过来使用，无需修改。

---

## 五、DNS 切换方案

### 5.1 推荐方案：低 TTL 平滑切换

```
第 1 步：将 genioai.appbobo.com 的 DNS TTL 降低到 60 秒
         （提前 24-48 小时操作，等旧 TTL 缓存过期）

第 2 步：确认国内服务器一切就绪
         - 服务部署完成
         - WireGuard 隧道连通
         - 生图流程测试通过（可通过 IP 直连测试）

第 3 步：修改 A 记录指向国内服务器 IP
         - 由于 TTL 为 60 秒，全球 DNS 约 1-5 分钟生效

第 4 步：在国内服务器申请 SSL 证书

第 5 步：验证正常后，将 TTL 恢复到正常值（600-3600 秒）
```

### 5.2 回滚方案

如果切换后出现问题，将 A 记录改回日本服务器 IP 即可，60 秒 TTL 下可以快速回滚。日本服务器上的 genioAI_server 在确认一切稳定前不要停止。

---

## 六、备案期间的过渡方案

如果 `appbobo.com` 尚未备案，备案审批需要 1-2 周，期间可以：

### 方案 A：IP 直连测试（推荐）

在备案期间先用 IP 地址直连国内服务器进行内部测试，域名暂时不切换。

```
测试地址：http://<国内服务器IP>:8200
正式地址：https://genioai.appbobo.com（仍指向日本，不影响线上用户）
```

- 线上用户不受影响
- 开发团队可以充分测试国内服务器
- 备案通过后再切换 DNS

### 方案 B：使用已备案域名

如果有其他已备案域名，可以临时使用该域名作为 API 入口：

```
临时方案：api.已备案域名.com  →  国内服务器 IP
后续迁移：备案通过后切换到 genioai.appbobo.com
```

需要修改前端的 API 地址配置，不够优雅，仅作为应急方案。

---

## 七、完整实施顺序

```
┌─────────────────────────────────────────────────┐
│ 第 1 步：确认备案状态                              │
│                                                   │
│   appbobo.com 是否已备案？                         │
│   ├── 已备案 → 直接进入第 2 步                     │
│   └── 未备案 → 提交备案申请（1-2 周）               │
│                备案期间同步执行第 2、3 步             │
└───────────────────────┬─────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────┐
│ 第 2 步：部署国内服务器                             │
│                                                   │
│   - 购买云服务器（2C4G）                           │
│   - 安装 Docker、Docker Compose                   │
│   - 部署 genioAI_server + MySQL + Redis + 前端     │
│   - 搭建 WireGuard 隧道                           │
│   - 迁移数据库数据                                  │
└───────────────────────┬─────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────┐
│ 第 3 步：内部测试                                  │
│                                                   │
│   - 通过 IP 直连测试所有功能                        │
│   - 测试生图完整流程                                │
│   - 测试登录、注册、积分等业务                       │
│   - 压力测试（可选）                                │
└───────────────────────┬─────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────┐
│ 第 4 步：DNS 切换（备案通过后）                      │
│                                                   │
│   - 降低 TTL 到 60 秒（提前 24-48 小时）            │
│   - 修改 A 记录指向国内服务器                        │
│   - 在国内服务器申请 Let's Encrypt 证书              │
│   - 验证 HTTPS 访问正常                             │
└───────────────────────┬─────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────┐
│ 第 5 步：清理与监控                                 │
│                                                   │
│   - 确认一切稳定后，停止日本服务器上的 genioAI_server │
│   - 恢复 DNS TTL 到正常值                           │
│   - 持续监控两台服务器的资源和 WireGuard 隧道状态     │
└─────────────────────────────────────────────────┘
```

---

## 八、总结

| 问题 | 结论 |
|------|------|
| 域名是否需要修改 | **不需要**，保持 `genioai.appbobo.com` 不变 |
| DNS 是否需要修改 | **需要**，A 记录从日本 IP 改到国内 IP |
| 是否需要 ICP 备案 | **需要**，`appbobo.com` 必须完成 ICP 备案 |
| 备案在哪里做 | 在购买国内服务器的云服务商处（阿里云/腾讯云） |
| 备案周期 | 1-2 周 |
| SSL 证书 | 在国内服务器重新申请 Let's Encrypt，免费 |
| 代码是否需要改动 | **不需要**，所有域名引用保持不变 |
| 前端/客户端是否需要更新 | **不需要**，对用户完全无感知 |
| 备案期间是否影响线上 | **不影响**，可通过 IP 直连测试，线上域名不动 |
