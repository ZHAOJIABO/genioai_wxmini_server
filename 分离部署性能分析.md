# GenioAI 分离部署后性能承载能力分析

## 一、核心限制参数汇总

| 参数 | 值 | 来源 |
|------|-----|------|
| genioAI_server gRPC 连接上限 | 1000 | `cmd/main.go:353` |
| MySQL 最大连接数 | 30 | `internal/db/mysql.go:48` |
| Redis 连接池 | 1000 | `internal/db/redis.go:14` |
| 每用户最大排队任务 | 2 | `quota/task_config_manager.go:28` |
| 每用户最大并发任务 | 1 | `quota/task_config_manager.go:28` |
| ai-brain worker 数量 | 5 | 环境变量 `TASK_WORKER_COUNT`，默认 5 |
| ai-brain 最大队列 | 1000 | 环境变量 `MAX_QUEUE_SIZE` |
| 状态轮询间隔 | 5 秒 | `provider_status_sync_processor.go:20` |
| 每批轮询任务数 | 10 | `provider_status_sync_processor.go:21` |
| 任务超时 | 15 分钟 | `provider_status_sync_processor.go:22` |

---

## 二、部署配置

分离部署后两台服务器的角色与配置：

| | 国内服务器（新购） | 日本服务器（现有） |
|------|-----------------|-----------------|
| **配置** | 2 vCPU / 4 GB / 50-80 GB SSD / 3-5 Mbps | 1 vCPU / 2 GB / 55 GB SSD |
| **部署服务** | genioAI_server + 前端 + MySQL + Redis | ai-brain + MySQL + Redis |
| **预估内存占用** | 600 MB - 1,100 MB（4 GB 充裕） | 530 MB - 750 MB（2 GB 足够） |
| **职责** | 处理用户请求、业务逻辑、图片后处理 | 调用海外 AI API、任务调度 |

---

## 三、生图任务吞吐量

生图任务的真正瓶颈不在服务器本身，而在 **ai-brain 的 5 个 worker** 和**外部 AI API 的响应时间**。

计算公式：

```
生图任务吞吐量 = worker 数量 / 平均任务耗时
```

### 3.1 各模型吞吐量

| AI 模型 | 平均耗时 | 5 workers 并发 | 每小时处理量 |
|---------|---------|--------------|------------|
| SiliconFlow (FLUX.1-schnell) | ~10-20 秒 | 5 张同时生成 | 900 - 1,800 张/小时 |
| Gemini 2.5 Flash | ~20-60 秒 | 5 张同时生成 | 300 - 900 张/小时 |
| Kling / Sora2 (视频) | 2-5 分钟 | 5 个同时生成 | 60 - 150 个/小时 |
| ComfyUI (复杂工作流) | 30-120 秒 | 5 个同时生成 | 150 - 600 张/小时 |

### 3.2 综合吞吐量

混合使用场景下：**每小时处理 200 - 600 个生图/生视频任务**。

注意：ai-brain 在等待 AI API 返回的过程中几乎不占本地 CPU/内存（网络 I/O 等待），因此 1C2G 的日本服务器完全够用，CPU 利用率通常只有 5-15%。

---

## 四、国内服务器 API 处理能力

### 4.1 各类请求 QPS 估算

Go 服务在 2 vCPU 下处理各类 API 请求的能力：

| 请求类型 | 预估 QPS | 说明 |
|---------|---------|------|
| 简单查询（配置、用户信息） | 200-500 QPS | MySQL 简单查询 + Redis 缓存 |
| 中等查询（任务列表、图片列表） | 100-200 QPS | 涉及分页查询和 JOIN |
| 图片压缩/处理 | 5-10 QPS | CPU 密集，单张处理耗时 100-500 ms |
| 提交生图任务 | 50-100 QPS | gRPC 调用 ai-brain + Redis 槽位检查 |

### 4.2 MySQL 连接池

- 最大 30 连接，每个查询占用约 1-10 ms
- 理论上限：30 连接 × (1000ms / 5ms 平均) ≈ 6,000 QPS
- 实际有锁争抢和慢查询，保守估计 **500-1,500 QPS**

---

## 五、同时在线用户承载

### 5.1 用户行为假设

| 行为 | 频率 |
|------|------|
| 浏览/查看（API 请求） | 每分钟 2-5 次 |
| 提交生图任务 | 每 5-10 分钟 1 次 |
| 图片上传/下载 | 每次生图前后各 1 次 |

### 5.2 不同负载下的承载

| 场景 | 同时在线用户 | 服务器压力 |
|------|------------|----------|
| 轻度使用（浏览为主） | **200-500 人** | CPU < 50%，内存 ~2 GB |
| 中度使用（频繁生图） | **100-200 人** | CPU 60-80%，内存 ~2.5 GB |
| 高峰期（集中生图） | **50-100 人** | CPU 接近满载 |

---

## 六、总体承载能力估算

| 指标 | 估算值 | 主要限制因素 |
|------|--------|------------|
| **同时在线** | **100-300 人** | 国内服务器 2 vCPU |
| **DAU（日活）** | **500-2,000 人** | 假设同时在线率 10-20% |
| **日生图量** | **3,000-10,000 张** | ai-brain 5 workers × 工作时间 |
| **日视频生成量** | **500-1,500 个** | 视频任务耗时更长 |
| **注册用户总量** | **10,000-50,000** | MySQL 存储，无瓶颈 |
| **系统并发生图** | **5 个** | ai-brain worker 数量 |
| **每用户并发** | **1 个处理中 + 2 个排队** | 代码中配置的配额 |

---

## 七、瓶颈排序

从最先遇到到最后遇到：

```
最先遇到                                              最后遇到
────────────────────────────────────────────────────────────────►

ai-brain        国内服务器      MySQL        gRPC连接     Redis
5 workers       2 vCPU         30 连接      1000 上限    1000 连接池
(系统并发=5)    (CPU密集操作)   (查询并发)    (远超需求)   (远超需求)
```

- **第一瓶颈：ai-brain 的 5 个 worker** —— 决定了整个系统的生图吞吐量上限。无论有多少用户在线，同一时刻最多只有 5 个任务在被 AI API 处理。
- **第二瓶颈：国内服务器 2 vCPU** —— 当大量用户同时做非生图操作（浏览、上传、图片压缩），CPU 会成为瓶颈。
- **第三瓶颈：MySQL 30 连接** —— 高并发下可能出现连接等待。

---

## 八、与分离前的对比

| 指标 | 分离前（1C2G 单机） | 分离后（国内 2C4G + 日本 1C2G） | 提升幅度 |
|------|-------------------|-------------------------------|---------|
| 同时在线 | 10-20 人 | 100-300 人 | **10-15 倍** |
| DAU | 50-100 人 | 500-2,000 人 | **10-20 倍** |
| API QPS | 5-15 | 200-500 | **30 倍+** |
| 日生图量 | ~1,500-2,000 张 | 3,000-10,000 张 | **3-5 倍** |
| 月带宽 | 3.94 GB（严重不足） | ~1 TB（国内 3 Mbps） | **250 倍** |
| 用户访问延迟 | 100-200 ms | 10-30 ms | **降低 80%+** |
| OOM 风险 | 极高 | 低 | 根本性解决 |

---

## 九、扩容路径

当用户量超过当前承载时，按优先级扩容：

### 阶段 0：零成本优化（立即可做）

| 优化项 | 操作 | 效果 |
|--------|------|------|
| 增加 ai-brain worker | `TASK_WORKER_COUNT` 从 5 调到 10-15 | 生图吞吐量翻 2-3 倍 |
| 降低 genioAI Redis 连接池 | 从 1000 降到 50-100 | 减少内存浪费约 50-100 MB |
| 调整 MySQL 连接数 | 按实际需要调整 | 减少连接开销 |
| 调整日志配置 | 500MB×50 备份改为 100MB×10 | 节省存储空间 |

### 阶段 1：DAU 2,000 - 5,000

| 操作 | 成本 |
|------|------|
| `TASK_WORKER_COUNT` 调到 10-15 | 0 元（改环境变量） |
| 数据库查询优化、添加索引 | 0 元 |

### 阶段 2：DAU 5,000 - 10,000

| 操作 | 成本 |
|------|------|
| 国内服务器升级到 4C8G | +100-200 元/月 |
| MySQL 连接数调到 50-60 | 0 元 |

### 阶段 3：DAU 10,000 - 30,000

| 操作 | 成本 |
|------|------|
| 日本服务器升级到 2C4G | +100 元/月 |
| `TASK_WORKER_COUNT` 调到 20-30 | 0 元 |
| 考虑 MySQL 读写分离 | 视方案而定 |

### 阶段 4：DAU 50,000+

| 操作 | 说明 |
|------|------|
| genioAI_server 多实例 + 负载均衡 | 需要引入 Nginx 负载均衡 |
| ai-brain 多实例部署 | 需要任务队列支持多消费者 |
| 数据库和缓存独立部署 | 使用云数据库服务 |
| 全面架构评审 | 重新评估架构 |

---

## 十、总结

分离部署后，系统在**不额外花钱**的情况下，仅通过调整 `TASK_WORKER_COUNT` 就能将生图吞吐量提升 2-3 倍。国内 2C4G 服务器可以稳定支撑 **100-300 人同时在线、500-2,000 DAU**，相比当前单机部署提升 10-20 倍。

后续用户增长时，扩容路径清晰且成本可控，前期主要通过调整配置参数即可应对，无需大规模架构改造。
