# GenioAI 生图平台分离部署方案

## 一、背景

### 1.1 当前部署架构

三个服务部署在同一台日本服务器上：

- **genioAI_server**：后端主服务（Go，gRPC + HTTP Gateway）
- **ai-brain**：AI 中台服务（Go，gRPC）
- **前端服务**：用户界面（Nginx/Node）

此外还包括各自的 MySQL、Redis 等基础设施。

### 1.2 当前服务器配置

| 资源 | 配置 |
|------|------|
| CPU | 1 vCPU |
| 内存 | 2048 MB (2 GB) |
| 存储 | 55 GB SSD |
| 带宽 | 3.94 GB/月 |
| 地区 | 日本 |

### 1.3 存在的问题

| 问题 | 说明 |
|------|------|
| 内存不足 | 所有进程合计需要 1,030 - 1,900 MB，随时可能触发 OOM |
| CPU 不足 | 1 vCPU 需承担两个 Go 服务 + 两个 MySQL + 图片处理，争抢严重 |
| 带宽不足 | 3.94 GB/月，生图平台每月最多传输约 2,000 张图片 |
| 用户延迟高 | 用户主要在国内，访问日本服务器延迟 100+ ms |

---

## 二、分离部署方案

### 2.1 目标架构

```
                          ┌─────────────────────────────┐
                          │        国内云服务器           │
                          │                             │
  国内用户 ──HTTP/S──────▶ │  前端服务 (Nginx)            │
                          │  genioAI_server (Go)        │
                          │  MySQL 8.0                  │
                          │  Redis                      │
                          └──────────┬──────────────────┘
                                     │
                              gRPC (WireGuard 隧道)
                                     │
                          ┌──────────▼──────────────────┐
                          │        日本服务器（现有）      │
                          │                             │
                          │  ai-brain (Go)              │
                          │  MySQL 8.0                  │
                          │  Redis                      │
                          └──────────┬──────────────────┘
                                     │
                              HTTP API 调用
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                ▼                ▼
               Google Gemini    Kling/Sora      SiliconFlow
               Google Imagen    MiniMax         ComfyUI
```

### 2.2 服务分配

| 服务器 | 部署的服务 | 理由 |
|--------|-----------|------|
| **国内服务器** | genioAI_server、前端、MySQL、Redis | 用户主要在国内，降低访问延迟 |
| **日本服务器** | ai-brain、MySQL、Redis | 调用海外 AI 模型（Gemini、Sora 等）网络更稳定 |

### 2.3 方案可行性依据

通过代码分析确认以下关键事实：

1. **通信协议**：genioAI_server 通过 gRPC 调用 ai-brain，定义在 `pkg/aibrain/v1/aibrain_service.proto`
2. **数据交换量小**：两个服务之间只传递 URL 和 JSON 元数据，**不传输图片/视频二进制数据**，每次 payload 仅几 KB
3. **通信是单向的**：始终由 genioAI_server 主动调用 ai-brain，ai-brain 不反向回调
4. **生图是异步任务**：提交任务后每 5 秒轮询一次状态，任务本身需要 30 秒到数分钟，跨境多 100-300 ms 延迟用户无感知

---

## 三、服务器选型

### 3.1 国内服务器（新购）

| 资源 | 建议配置 | 理由 |
|------|---------|------|
| CPU | 2 vCPU | Go 服务 + MySQL + 图片压缩处理 |
| 内存 | 4 GB | genioAI_server + MySQL + Redis + 前端 |
| 存储 | 50-80 GB SSD | 数据库增长 + 日志存储 |
| 带宽 | 3-5 Mbps 或按量付费 | 3 Mbps ≈ 每月约 1 TB 理论上限 |

预估资源占用：

| 进程 | 内存估算 |
|------|---------|
| genioAI_server | 100-200 MB |
| MySQL 8.0 | 300-500 MB |
| Redis | 50-100 MB |
| 前端 (Nginx) | 50-100 MB |
| Docker 引擎 | 100-200 MB |
| **合计** | **600 MB - 1,100 MB** |

4 GB 内存充裕，留有足够余量。

参考价格（轻量应用服务器）：
- 阿里云 / 腾讯云 2C4G：约 100-200 元/月

### 3.2 日本服务器（现有）

ai-brain 独占后的资源占用：

| 进程 | 内存估算 |
|------|---------|
| ai-brain | 100-200 MB |
| MySQL 8.0 | 300-400 MB |
| Redis | 30-50 MB |
| Docker 引擎 | 100 MB |
| **合计** | **530 - 750 MB** |

现有 1 vCPU / 2 GB 配置对 ai-brain 单独运行足够，因为它主要是调用外部 API 并等待结果，CPU 和内存消耗不大。

---

## 四、网络方案

### 4.1 WireGuard 加密隧道（推荐）

使用 WireGuard 在两台服务器之间建立点对点加密隧道：

```
国内服务器 (10.0.0.1) ◄────WireGuard 隧道────► 日本服务器 (10.0.0.2)
```

优点：
- 免费开源，性能优秀（内核级实现）
- 自带加密，gRPC 无需额外配置 TLS
- 配置简单，仅需几行配置文件
- 断线自动重连

#### 日本服务器 WireGuard 配置

```ini
# /etc/wireguard/wg0.conf
[Interface]
PrivateKey = <日本服务器私钥>
Address = 10.0.0.2/24
ListenPort = 51820

[Peer]
PublicKey = <国内服务器公钥>
AllowedIPs = 10.0.0.1/32
```

#### 国内服务器 WireGuard 配置

```ini
# /etc/wireguard/wg0.conf
[Interface]
PrivateKey = <国内服务器私钥>
Address = 10.0.0.1/24

[Peer]
PublicKey = <日本服务器公钥>
Endpoint = <日本服务器公网IP>:51820
AllowedIPs = 10.0.0.2/32
PersistentKeepalive = 25
```

启动隧道：

```bash
# 两台服务器分别执行
sudo wg-quick up wg0

# 设置开机自启
sudo systemctl enable wg-quick@wg0
```

### 4.2 防火墙配置

日本服务器只需开放：
- **51820/UDP**：WireGuard 隧道端口（仅允许国内服务器 IP）
- ai-brain 的 8080 端口**不暴露到公网**，只监听 WireGuard 内网地址

```bash
# 日本服务器防火墙示例 (ufw)
sudo ufw allow from <国内服务器公网IP> to any port 51820 proto udp
sudo ufw deny 8080
```

---

## 五、代码改动

### 5.1 genioAI_server 配置修改

修改文件：`conf/server.prod.yaml`

```yaml
# 修改前（同机部署）
AIBrainServer:
  Addr: ai-brain-core-app:8080

# 修改后（跨服务器，通过 WireGuard 隧道）
AIBrainServer:
  Addr: 10.0.0.2:8080
```

### 5.2 Docker 网络调整

**国内服务器** `docker-compose.yml`：移除 `genio-shared` 外部网络（不再需要和 ai-brain 共享 Docker 网络）

```yaml
networks:
  genio-backend-network:
    driver: bridge
  # 移除 genio-shared 网络
```

genioAI_server 容器需要能访问宿主机的 WireGuard 网络，两种方式：
- 方式 A：容器使用 `network_mode: host`
- 方式 B：容器使用默认 bridge 网络，genioAI_server 配置中 `Addr` 改为 `host.docker.internal:8080`，宿主机做端口转发

推荐方式 B，保持容器网络隔离。在宿主机添加转发规则：

```bash
# 国内服务器：将本地 8080 转发到 WireGuard 对端的 ai-brain
sudo iptables -t nat -A PREROUTING -i docker0 -p tcp --dport 8080 -j DNAT --to 10.0.0.2:8080
sudo iptables -A FORWARD -p tcp -d 10.0.0.2 --dport 8080 -j ACCEPT
```

或者更简单的方案——在 genioAI_server 容器中直接添加 `extra_hosts`：

```yaml
services:
  backend:
    extra_hosts:
      - "ai-brain-core-app:10.0.0.2"
    # 这样 conf/server.prod.yaml 中的 Addr 可以保持 ai-brain-core-app:8080 不变
```

### 5.3 ai-brain Docker 网络调整

**日本服务器** `docker-compose.prod.yml`：ai-brain 监听 WireGuard 内网地址

```yaml
services:
  ai-brain:
    ports:
      - "10.0.0.2:8080:8080"   # 只监听 WireGuard 接口，不暴露公网
    networks:
      - ai-brain-network

networks:
  ai-brain-network:
    driver: bridge
  # 移除 genio-shared 网络
```

### 5.4 建议的 gRPC 连接增强

修改文件：`internal/service/ai_clients/aigc_core/client.go`

跨公网连接建议添加以下 gRPC 配置：

```go
import "google.golang.org/grpc/keepalive"

conn, err := grpc.Dial(
    config.Addr,
    grpc.WithTransportCredentials(insecure.NewCredentials()), // WireGuard 已加密
    grpc.WithKeepaliveParams(keepalive.ClientParameters{
        Time:                30 * time.Second, // 每 30 秒发送 keepalive ping
        Timeout:             10 * time.Second, // ping 超时时间
        PermitWithoutStream: true,             // 无活跃流时也发送 keepalive
    }),
    grpc.WithDefaultCallOptions(
        grpc.MaxCallRecvMsgSize(10*1024*1024), // 10 MB 接收上限
    ),
)
```

---

## 六、图片流量路径

### 6.1 当前流转路径

```
ai-brain 调用 AI API 生图
    ↓
AI API 返回图片 URL
    ↓
ai-brain 将 URL 通过 gRPC 返回给 genioAI_server（仅传 URL，几 KB）
    ↓
genioAI_server 通过 HTTP 下载图片（跨境流量）
    ↓
genioAI_server 处理图片（压缩/转格式）
    ↓
genioAI_server 上传到 Cloudflare R2
    ↓
用户从 cdn.appbobo.com (Cloudflare CDN) 获取图片
```

### 6.2 优化建议

genioAI_server（国内）下载海外 AI API 生成的图片时会走跨境流量。可选优化：

- **方案 A**：保持现状。Cloudflare R2 上传走国际线路，但只在服务端后台执行，用户无感知。最终用户从 CDN 获取，体验不受影响。
- **方案 B**：将图片下载和上传逻辑移到 ai-brain 侧（日本），ai-brain 直接将图片上传到 R2，然后只把 CDN URL 返回给 genioAI_server。减少跨境流量，但需要改造代码。

---

## 七、延迟影响分析

### 7.1 用户直接感知的操作

| 操作 | 改善前（日本服务器） | 改善后（国内服务器） |
|------|-------------------|-------------------|
| 页面加载 | 100-200 ms | 10-30 ms |
| 登录/注册 | 100-200 ms | 10-30 ms |
| API 请求（非生图） | 100-200 ms | 10-30 ms |
| 图片浏览（CDN） | 取决于 CDN 节点 | 不变 |

### 7.2 生图任务（异步，用户无感知）

| 阶段 | 延迟变化 | 影响 |
|------|---------|------|
| 提交任务 (gRPC) | +100-300 ms | 无感知，用户提交后立即返回 |
| 状态轮询 (每 5 秒) | +100-300 ms | 无感知，后台自动执行 |
| 图片下载 (HTTP) | 可能增加 | 后台处理，不阻塞用户 |
| AI API 调用 | 不变 | ai-brain 仍在日本，直连海外 API |

**结论**：生图任务本身耗时 30 秒到数分钟，增加的 100-300 ms 跨境延迟完全可忽略。

---

## 八、风险与应对

### 8.1 跨境网络不稳定

**风险**：中日之间网络偶尔出现延迟波动或短暂不通。

**应对措施**：
- WireGuard 内置断线重连机制
- gRPC keepalive 保持长连接活跃
- genioAI_server 端添加重试逻辑和超时配置
- 考虑添加断路器（circuit breaker），ai-brain 不可用时给用户友好提示

### 8.2 数据一致性

**风险**：两个服务使用独立的 MySQL，数据分布在两个地域。

**应对措施**：
- genioAI_server 的 MySQL 存储用户数据、订单、积分等业务数据
- ai-brain 的 MySQL 只存储异步任务记录（`aibrain_async_tasks` 表）
- 两个数据库职责清晰，不存在跨库事务需求
- 无需数据同步

### 8.3 国内服务器备案

**风险**：国内云服务器绑定域名需要 ICP 备案。

**应对措施**：
- 使用已备案域名，或提前进行备案（通常需要 1-2 周）
- 备案期间可先用 IP 直连测试

---

## 九、实施步骤

### 第一步：购买国内云服务器

- 配置：2 vCPU / 4 GB / 50 GB SSD / 3-5 Mbps
- 推荐：阿里云或腾讯云轻量应用服务器
- 安装 Docker 和 Docker Compose

### 第二步：搭建 WireGuard 隧道

1. 两台服务器安装 WireGuard
2. 生成密钥对
3. 配置隧道并测试连通性

```bash
# 测试隧道连通
ping 10.0.0.2  # 从国内服务器 ping 日本服务器
```

### 第三步：部署国内服务

1. 在国内服务器部署 genioAI_server + MySQL + Redis + 前端
2. 修改 `conf/server.prod.yaml` 中的 `AIBrainServer.Addr`
3. 修改 `docker-compose.yml` 网络配置（添加 `extra_hosts` 或调整网络模式）
4. 迁移 MySQL 数据（从日本服务器导出 genioAI_server 的数据库并导入）

```bash
# 日本服务器导出
mysqldump -u genio_backend_user -p genio_backend_db > genio_backup.sql

# 国内服务器导入
mysql -u genio_backend_user -p genio_backend_db < genio_backup.sql
```

### 第四步：精简日本服务器

1. 停止日本服务器上的 genioAI_server、前端、对应的 MySQL 和 Redis
2. 调整 ai-brain 的 Docker 网络配置（移除 genio-shared 网络）
3. 配置 ai-brain 仅监听 WireGuard 内网地址

### 第五步：DNS 切换

1. 将前端和 API 域名解析到国内服务器 IP
2. 确认国内备案（如需要）

### 第六步：验证与监控

1. 测试完整生图流程：提交 → 轮询 → 获取结果
2. 测试登录、注册、积分等业务功能
3. 监控 WireGuard 隧道稳定性
4. 监控两台服务器的资源使用情况

---

## 十、成本对比

| 项目 | 当前 | 分离后 |
|------|------|--------|
| 日本服务器 | 现有费用（维持） | 不变 |
| 国内服务器 | 无 | +100-200 元/月 |
| 带宽 | 3.94 GB/月（不足） | 国内 3-5 Mbps（充裕） |
| **总增加成本** | - | **约 100-200 元/月** |
| **收益** | - | 用户体验大幅提升 + 解决资源瓶颈 |
